# Polyphonic detection dependencies (optional)
#
# basic-pitch metadata pins tensorflow<2.15.1, which pip cannot satisfy on Python 3.13+.
# Install this file first, then install basic-pitch without deps:
#
#   pip install -r requirements-poly.txt
#   pip install basic-pitch==0.4.0 --no-deps
#
# Or run:  powershell -File scripts/install-poly.ps1

mir-eval>=0.6
pretty-midi>=0.2.9
resampy>=0.2.2,<0.4.3
onnxruntime>=1.17.0
