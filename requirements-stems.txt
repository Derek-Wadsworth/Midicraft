# Stem separation (optional)
# Pin matching torch/torchaudio — mismatched versions (e.g. torch 2.12 + torchaudio 2.11) break installs.
demucs>=4.0.0,<5.0.0
torch>=2.5.0,<2.12.0
torchaudio>=2.5.0,<2.12.0
